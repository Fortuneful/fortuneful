import BusinessLanding from "./BusinessLanding";

export const metadata = {
  title: "Fortuneful — One dashboard to run and grow your business",
  description: "Everything your business needs. One dashboard. AI where it helps, humans where they matter. Websites, ads, menus, bookkeeping, hiring — request it, and it gets done.",
};

export default function BusinessPage() {
  return <BusinessLanding />;
}
